<!--
The title of this PR becomes a line in the next release's notes, so write it as
something a player would understand — "Six game statuses, replacing the
four-status vocabulary", not "refactor status enum".

Everything below is the detail that does not fit on that line. It stays one
click away from the release page, so it is worth writing properly.

Apply one label: enhancement, changed, bug, removed, database, documentation,
or chore (chore keeps the PR out of the release notes entirely).
-->

## What changed

## Why

## Before you upgrade

<!--
Anything that happens to a library that already exists — migrations, values that
are reinterpreted, settings that move. Delete this heading if nothing does.
-->
